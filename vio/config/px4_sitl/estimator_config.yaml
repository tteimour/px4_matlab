%YAML:1.0 # need to specify the file type at the top!

verbosity: "INFO" # ALL, DEBUG, INFO, WARNING, ERROR, SILENT

use_fej: true # if first-estimate Jacobians should be used (enable for good consistency)
integration: "rk4" # discrete, rk4, analytical
use_stereo: false # mono camera — we only have one camera
max_cameras: 1 # mono mode for iris_vio downward-facing camera

calib_cam_extrinsics: true # let filter refine extrinsics from data (initial values in kalibr_imucam_chain.yaml)
calib_cam_intrinsics: false # we know exact Gazebo pinhole parameters
calib_cam_timeoffset: true # allow small time offset calibration
calib_imu_intrinsics: false # not needed for simulation
calib_imu_g_sensitivity: false # not needed for simulation

max_clones: 11
max_slam: 50
max_slam_in_update: 25
max_msckf_in_update: 10
dt_slam_delay: 2

gravity_mag: 9.8066 # from iris_vio.world physics settings

feat_rep_msckf: "GLOBAL_3D"
feat_rep_slam: "GLOBAL_3D"
feat_rep_aruco: "GLOBAL_3D"

# zero velocity update parameters
try_zupt: true # enable ZUPT for better handling of stationary startup
zupt_chi2_multipler: 1
zupt_max_velocity: 0.1
zupt_noise_multiplier: 1
zupt_max_disparity: 0 # imu-based ZUPT
zupt_only_at_beginning: false

# ==================================================================
# Initialization parameters
# ==================================================================

init_window_time: 2.0
init_imu_thresh: 1.0
init_max_disparity: 1.5
init_max_features: 15

init_dyn_use: true # dynamic initialization (drone must be in motion)
init_dyn_mle_opt_calib: false
init_dyn_mle_max_iter: 50
init_dyn_mle_max_time: 0.5
init_dyn_mle_max_threads: 6
init_dyn_num_pose: 6
init_dyn_min_deg: 5.0 # reduced from 15.0 — SITL drone needs less rotation to init

init_dyn_inflation_ori: 10
init_dyn_inflation_vel: 100
init_dyn_inflation_bg: 10
init_dyn_inflation_ba: 100
init_dyn_min_rec_cond: 1e-15

init_dyn_bias_g: [0.0, 0.0, 0.0] # gyroscope bias guess (Gazebo bias_mean for gyro is small: 7.5e-6)
init_dyn_bias_a: [0.0, 0.0, 0.0] # accelerometer bias guess — matches iris_vio.sdf bias_mean=0.0

# ==================================================================
# Output / logging
# ==================================================================

record_timing_information: false
record_timing_filepath: "/tmp/traj_timing.txt"

save_total_state: false
filepath_est: "/tmp/ov_estimate.txt"
filepath_std: "/tmp/ov_estimate_std.txt"
filepath_gt: "/tmp/ov_groundtruth.txt"

# ==================================================================
# Feature tracking parameters
# ==================================================================

use_klt: true
num_pts: 200 # reduced for 640x480 (vs 250 for 752x480)
fast_threshold: 15
grid_x: 16 # grid adapted for 640px width
grid_y: 12 # grid adapted for 480px height
min_px_dist: 12 # reduced for smaller resolution
knn_ratio: 0.70
track_frequency: 30.0 # matches iris_vio camera update_rate
downsample_cameras: false
num_opencv_threads: 4
histogram_method: "HISTOGRAM" # NONE, HISTOGRAM, CLAHE

# aruco tag tracker — disabled
use_aruco: false
num_aruco: 1024
downsize_aruco: true

# ==================================================================
# Measurement noise
# ==================================================================

up_msckf_sigma_px: 1
up_msckf_chi2_multipler: 1
up_slam_sigma_px: 1
up_slam_chi2_multipler: 1
up_aruco_sigma_px: 1
up_aruco_chi2_multipler: 1

use_mask: false

# imu and camera spacial-temporal config files (relative to this file's directory)
relative_config_imu: "kalibr_imu_chain.yaml"
relative_config_imucam: "kalibr_imucam_chain.yaml"
